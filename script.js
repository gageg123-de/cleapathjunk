const navbar=document.querySelector('#navbar'),menuToggle=document.querySelector('.menu-toggle'),mobileMenu=document.querySelector('#mobileMenu'),range=document.querySelector('#baRange'),baAfter=document.querySelector('#baAfter'),baDivider=document.querySelector('#baDivider'),baHandle=document.querySelector('#baHandle'),form=document.querySelector('#estimateForm'),formSuccess=document.querySelector('#formSuccess');const setScrolled=()=>navbar.classList.toggle('is-scrolled',window.scrollY>12);setScrolled();window.addEventListener('scroll',setScrolled,{passive:true});menuToggle.addEventListener('click',()=>{const open=mobileMenu.classList.toggle('is-open');menuToggle.setAttribute('aria-expanded',String(open));menuToggle.textContent=open?'Close':'Menu'});mobileMenu.addEventListener('click',event=>{if(event.target.matches('a')){mobileMenu.classList.remove('is-open');menuToggle.setAttribute('aria-expanded','false');menuToggle.textContent='Menu'}});const updateBeforeAfter=value=>{baAfter.style.clipPath='inset(0 0 0 '+value+'%)';baDivider.style.left=value+'%';baHandle.style.left=value+'%'};range.addEventListener('input',event=>updateBeforeAfter(event.target.value));updateBeforeAfter(range.value);const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}})},{threshold:.14});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));form.addEventListener('submit',event=>{event.preventDefault();formSuccess.classList.add('is-visible');form.reset()});